apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - api.{{ cluster_name }}.{{ cluster_base_domain }}
      servingCertificate:
        name: {{ api_tls_secret_name }}